#built in functions

# interables filter, map, sorted, enumerate, zip

#filter(function, iter) - creates a new iterable for which the function is true
# map(function, iter) calls function on each element on iter, returns iter

#sorted(iter, key=None, reverse = False) - returns a sorted iterable; output is a list
# enumerate = returns a series of tuples with (n, value) 

# zip - creates tuples for each iterable that gets "zipped up" (think combining two arrays into one)


# all(iter) - returns true if all items are true
# any - same but if any 


# analyze len, max, min, sum, any, all